- [OpenAPI integer/format rules](openapi-rules.md) — All integers must use `type: number`; no `format: email` or `format: uri`; inline request bodies generate duplicate exports — always use `$ref` to named schemas.
- [DaymarkCharacter v2 system](character-system.md) — All mascot assets in `attached_assets/characters/v2/`; vivid purple palette; centralized component; spec-correct pose map per screen.
- [Scrapbook component library](scrapbook-components.md) — Shared scrapbook/Memory Shelf components live in `artifacts/daymark/src/components/scrapbook.tsx`; PhysicalGiftAnimation is the step-7 success component.
- [Asset paths](asset-paths.md) — Generated images are at `attached_assets/generated_images/`; v2 mascots at `attached_assets/characters/v2/`; resolved via `@assets` alias in Vite config.
- [DB schema additions](db-schema.md) — `onboardingCompleted: boolean` added to usersTable; `notificationsTable` added; always run raw SQL via pg pool (not drizzle-kit push) in non-TTY env; `--force` flag does NOT bypass TTY requirement.
- [Calendar security fix](calendar-security.md) — `/calendar/events` and `/calendar/on-this-day` now auth-guarded and userId-scoped (was the critical security gap).
- [Supabase Auth migration](supabase-auth.md) — Clerk fully removed; Supabase Auth via sb-token cookie + Bearer; JIT provisioning in requireAuth; useAppAuth() provides session/user/signOut.
- [Shared memory permissions](shared-memory-permissions.md) — canViewMemory/canEditMemory/canDeleteMemory/canInviteToMemory helpers in artifacts/api-server/src/routes/memory-participants.ts; participants route handles invite/accept/decline/remove.
- [Social feature tables](social-features-schema.md) — connections, relationship_streaks, streak_activities, scheduled_messages, memory_drops, memory_share_links, daily_prompts, prompt_responses all applied via raw SQL; usersTable extended with username/displayName/bio/birthday/timezone/city/discoverableBy* columns; memoriesTable extended with visibility + globe* columns; peopleTable extended with linkedUserId.
- [DB rebuild method](db-rebuild.md) — @workspace/db has NO build script; exports directly from src/index.ts; esbuild in api-server handles TS compilation; drizzle-kit push requires TTY so use raw pg pool SQL for non-interactive migration.
- [Scheduler](scheduler.md) — Message delivery worker at artifacts/api-server/src/lib/scheduler.ts; started in index.ts after app.listen(); polls every 60s; yearly repeat creates next year's message.
- [DayLink streaks](daylink-streaks.md) — canonicalPair() ensures userAId < userBId lexicographically; unique constraint (streak_id, activity_date) prevents double-counting; grace day activates after 14+ days and only once; milestone notifications at 7/30/100/365.
- [New routes](new-api-routes.md) — /api/connections, /api/users/search, /api/daylinks, /api/daylinks/:userId, /api/messages, /api/globe/memories, /api/memories/:id/publish-globe, /api/memories/:id/globe-settings, /api/drops, /api/prompts/today, /api/prompts/:id/respond all wired in routes/index.ts.
- [New pages](new-pages.md) — /connections (ConnectionsPage), /messages (MessagesPage), /globe (GlobePage) all added to App.tsx; home IA reordered per spec (This Day → Daylinks → Daily Question → Capture → Globe → Coming Up → People); profile has stats+bio+username; gift-detail has full share sheet + globe publish flow.
- [Toast hook path](toast-hook.md) — useToast lives at `@/hooks/use-toast` NOT `@/components/ui/use-toast`.
- [Home summary API](home-summary-api.md) — GET /home/summary prefers on-this-day memories (same MM-DD, prior year) for giftFromPast; falls back to most recent; frontend labels with year delta and "X years ago today".
- [Profile PATCH](profile-patch.md) — PATCH /auth/profile handles firstName/lastName/profileImageUrl/username/displayName/bio/birthday/timezone/city/discoverableByUsername/discoverableByEmail; username validated 3-24 chars [a-z0-9_]; uniqueness checked inline.
- [Clerk auth in social routes](clerk-social-routes.md) — All social routes (connections, daylinks, globe, memory-drops, scheduled-messages, share-links) had stale Passport req.isAuthenticated(); replaced with getAuth(req) from @clerk/express; pattern: `const userId = getAuth(req)?.sessionClaims?.userId || getAuth(req)?.userId`; requireAuth() now returns string|null not boolean.
- [Share card renderer](share-card-renderer.md) — Canvas-based share card at `artifacts/daymark/src/components/share-card-renderer.tsx`; 3 templates (gift/polaroid/on_this_day), 3 formats (story/portrait/square); renders to offscreen canvas, downloads as PNG or uses Web Share API with files.
- [Profile photo upload](profile-photo.md) — Profile EditProfileSheet uses useUpload from @workspace/object-storage-web; photo uploaded to object storage, objectPath saved, PATCH /auth/profile sends profileImageUrl = `/api/storage${objectPath}`.
- [Memory Drop UI](memory-drop-ui.md) — DropComposeSheet + ReceivedDropCard in person-detail.tsx; Drop a moment button + received drops list rendered after DayLink card when person.linkedUserId exists; AnimatePresence wraps compose sheet at bottom of page.
- [Secure share links](secure-share-links.md) — POST /api/memories/:id/share-link creates 64-char token; gift-detail Copy Link calls this instead of copying private URL; public /m/:token served by SharedMemoryPage with safe-fields-only DTO.
- [Spec 3 implementation](spec3-implementation.md) — Tracks done/outstanding for the 50-part production-readiness pass; key additions: relationship_events, birthday_wishes, invites, monthly_capsules tables; Leaflet globe; Connections Polaroid redesign; Messages envelope redesign; CapsulePage; InvitePage; scheduler birthday reminders + monthly capsule generation.
- [Production pass phase 2](production-pass-phase2.md) — All non-Supabase phases complete: privacy/notif columns on users, relationship_event_reminders table, DELETE account, data export, rate limiting, terms/privacy pages, birthday wish wall, scheduler dedup via reminders table. Supabase blocked on credentials.
